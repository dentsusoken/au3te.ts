import { describe, it } from 'vitest';

describe('BaseEndpoint', () => {
  describe('extractClientCertificateChain', () => {
    it('cannot access because this is protected method', () => {});
  });
});
